import {AdminTheme} from 'meteor/dasdeck:admin-theme';


AdminTheme.setNavigation([
    {
        route:"/users",
        label:"manage users",
        class:"fa fa-users"
    }
]);

AdminTheme.setHeader('FRIDA');

